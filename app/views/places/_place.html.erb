<div class ="container">
<% obj.each do |place| %>
	<div class="col-md-4">
      <div class="thumbnail">
      	<%= image_tag place.picture, style: "width:100%" %>
          <div class="caption">
            <p><%= link_to place.name, place_path(place)%>: <%= truncate(place.description, length: 50)%>... País: <%= place.country%>, cidade: <%= place.city%> <%= "R$#{place.price}"%></p>
          </div>
        </a>
      </div>
    </div>
<% end %>
</div>